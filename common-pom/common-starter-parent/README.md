# Common Starter Parent

The Maven `<parent>` for every module in this repository. Extends `spring-boot-starter-parent` (Java 21 compiler release, resource filtering, plugin defaults) and imports the `common-dependencies` BOM. Not a runtime dependency.
