# Common Dependencies

A plain BOM: `dependencyManagement` only, no build configuration. Holds shared dependency versions not already managed by `spring-boot-dependencies` (currently `springdoc-openapi-bom`).
