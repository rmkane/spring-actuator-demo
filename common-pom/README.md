# Common POM

Aggregator for the shared Maven parent and dependency-management BOM. Contains no shared configuration itself.

- [common-starter-parent](common-starter-parent/) — the actual `<parent>` every module inherits from.
- [common-dependencies](common-dependencies/) — dependency-management BOM for versions not already managed by Spring Boot.
